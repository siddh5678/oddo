<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Maintenance Team Tree View -->
    <record id="view_maintenance_team_tree" model="ir.ui.view">
        <field name="name">maintenance.team.tree</field>
        <field name="model">maintenance.team</field>
        <field name="arch" type="xml">
            <tree string="Maintenance Teams">
                <field name="name"/>
                <field name="technician_ids" widget="many2many_tags"/>
            </tree>
        </field>
    </record>

    <!-- Maintenance Team Form View -->
    <record id="view_maintenance_team_form" model="ir.ui.view">
        <field name="name">maintenance.team.form</field>
        <field name="model">maintenance.team</field>
        <field name="arch" type="xml">
            <form string="Maintenance Team">
                <sheet>
                    <group>
                        <group>
                            <field name="name" required="1"/>
                        </group>
                        <group>
                            <field name="active" widget="boolean_toggle"/>
                        </group>
                    </group>
                    <group string="Team Members">
                        <field name="technician_ids" widget="many2many_tags" 
                               domain="[('is_technician', '=', True)]"
                               options="{'no_create': True}"/>
                        <p class="text-muted">
                            Select technicians who are members of this maintenance team.
                            Only team members can be assigned to maintenance requests for equipment assigned to this team.
                        </p>
                    </group>
                    <group string="Description">
                        <field name="description" placeholder="Team description and responsibilities..."/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Maintenance Team Search View -->
    <record id="view_maintenance_team_search" model="ir.ui.view">
        <field name="name">maintenance.team.search</field>
        <field name="model">maintenance.team</field>
        <field name="arch" type="xml">
            <search string="Maintenance Teams">
                <field name="name"/>
                <filter string="Active" name="active" domain="[('active', '=', True)]"/>
            </search>
        </field>
    </record>

    <!-- Maintenance Team Action -->
    <record id="action_maintenance_team_view" model="ir.actions.act_window">
        <field name="name">Maintenance Teams</field>
        <field name="res_model">maintenance.team</field>
        <field name="view_mode">tree,form</field>
        <field name="search_view_id" ref="view_maintenance_team_search"/>
    </record>
</odoo>

